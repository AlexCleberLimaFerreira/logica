const dobrar = function(num) {    // função anônima sempre é atribuída a uma variá
                                  // função anônima é uma função que não tem nome, mas pode ser atribuída a uma variável
    return num * 2

}

console.log(dobrar(15));
